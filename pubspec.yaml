name: Relife
description: A new Flutter project

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  flutter_map: ^6.1.0
  file_picker: ^8.0.0        # For picking videos
  video_player: ^2.5.1      # For video playback
  path_provider: ^2.0.15    # ✅ only one version
  universal_html: ^2.0.8    # For web file handling
  video_thumbnail: ^0.5.2   # For generating video thumbnails
  image_picker: ^1.1.0
  google_fonts: ^6.2.1
  latlong2: ^0.9.0
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.1
  razorpay_flutter: ^1.3.0
  google_maps_flutter: ^2.13.1
  geolocator: ^10.0.0
  geocoding: ^2.1.1
  dio: ^5.9.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  flutter_launcher_icons: ^0.13.1


flutter:
  uses-material-design: true
  assets:
    - assets/videos/videoplayback.mp4
    - assets/videos/disaster1.mp4
    - assets/videos/disaster2.mp4
    - assets/videos/disaster3.mp4
    - assets/videos/disaster4.mp4
    - assets/videos/disaster5.mp4
    - assets/images/logo.png
  
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
